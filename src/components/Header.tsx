import React from 'react'
import { CompanyDetails } from '../types/CompanyDetails';

interface HeaderProps {
        companyDetails: CompanyDetails;
}

export const Header: React.FC<HeaderProps> = ({companyDetails}) => {
        return (<></>);
}